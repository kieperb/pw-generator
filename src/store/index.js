import Vue from 'vue';
import Vuex from 'vuex';
import pwgen from '@/api/pw-gen';

Vue.use(Vuex);

export default new Vuex.Store({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
  }
});
